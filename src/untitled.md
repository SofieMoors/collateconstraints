# Cerrect visualization of characters (cfr. MUFI)

## First, install the `jupyterlab-fonts` extension
`pip install jupyterlab-fonts`
### or
`conda install -c conda-forge jupyterlab-fonts`

## Second, 
